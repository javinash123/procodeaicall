# Production Environment Configuration for AIAgent

# Node.js Environment
NODE_ENV=production

# Server Port (running behind Apache proxy)
PORT=3000

# MongoDB Connection String
# Replace with your actual MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/aiagent?retryWrites=true&w=majority

# Session Secret (use a strong random value)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_secure_random_session_secret_here

# Optional: API Configuration
# API_URL=http://3.208.52.220/aiagent/api
